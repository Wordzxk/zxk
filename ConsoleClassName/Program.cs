using System;
using System.IO;
using System.Reflection;

namespace ConsoleClassName
{
    class Program
    {
       /* static void Main(string[] args)
        {
            Type target = Type.GetType("ConditionComplier.Student");
            object oTarget = target.GetConstructor(new Type[0]).Invoke(null);



            Role[] roles = { Role.Sthdent, Role.Teacher };




            //Type tStudent = typeof(Student);
            //Console.WriteLine(tStudent.GetField("_age",
            //    BindingFlags.NonPublic | BindingFlags.Instance)
            //    .GetValue(new Student()));

        }*/
    }
}
