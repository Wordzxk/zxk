using System;
using System.Collections.Generic;
using System.Text;

namespace ConsoleClassName.Array
{
    class ClassSelect
    {
       /* static void bunnle_sort(int[] unsorted)
        {
            //这一层for循环，决定接受变量（最后一个可不参加）
            for (int i = 0; i < unsorted.Length-1; i++)
            {
                int min = unsorted[i];                    //声明一个保存最小值的变量 
                int minIndex = i;                         //声明一个保存最小值下标的变量

                //这层循环，选择出数组中的最小值（所有元素都要参加比较）
                for (int j = 0; j < unsorted.Length; j++)
                {
                    if (unsorted[j] < min)
                    {
                        min = unsorted[j];
                        minIndex = j;
                    }
                }
                //最小值与下标为i的数组变量进行交换
                int temp = unsorted[i];
                unsorted[i] = min;
                unsorted[minIndex] = temp;
            }
        }
        static void Main(string[] args)
        {
            int[] x = { 6, 2, 4, 1, 5, 9, 8, 3 };         //声明一个整数组，并赋值
            //冒泡前排
            Console.Write("排序前：");
            foreach (var item in x)
            {
                Console.Write(item + "");
            }
            bunnle_sort(x);                               //调用冒泡排序方法
            //冒泡后数组
            Console.Write("\n排序后：");
            foreach (var item in x)
            {
                Console.Write(item + "");
            }
            Console.ReadLine();

        }*/


    }
}
