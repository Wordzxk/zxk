@page
@model WebThreetier.Pages.Suggest.IndexModel
@{
    ViewData["Title"] = "文章单页";
}

<h4>@Model.Blogs.Title</h4>
<p>作者：@Model.Blogs.Author.Name</p>


<main>
    @Html.Raw(Model.Blogs.Body)
</main>

<p>@ViewData["id"]</p>
@*@foreach (var item in Model.Blogs)
{
    <h5>@item.Title</h5>
    <main>@Html.Raw(item.Body)</main>
    }
<p>@ViewData["id"]</p>*@

