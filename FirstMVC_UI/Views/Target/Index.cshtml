@using FirstMVC_UI.Models.TargerModel
@model FirstMVC_UI.Models.TargerModel.TargetModel
@{
}

<h1>发布目标</h1>
@using (Html.BeginForm())
{
 

    for (int i = 0; i < Model.Messages.Count; i++)
    {
        
        <div>
            @Html.CheckBoxFor(m=>Model.Messages[i].Selected)
            @Model.Messages[i].Content
            @Html.HiddenFor(m=>Model.Messages[i])
        </div>
    }
    <div>
        <label>标题(必填)</label><br />
        @Html.TextAreaFor(m => m.Headline)
    </div>

    <div>
        <lable>详细说明:@Html.TextAreaFor(m => m.Explain)</lable>
    </div>

    <div>
        <label>说明:</label>@Html.TextAreaFor(m => m.Label)
    </div>
    <div>
        <label><input type="checkbox" name="Remember" value="1" />一</label>
        <label><input type="checkbox" name="Remember" value="2" />二</label>
        <label><input type="checkbox" name="Remember" value="3" />三</label>
        <label><input type="checkbox" name="Remember" value="4" />四</label>
        <label><input type="checkbox" name="Remember" value="5" />五</label>
        <label><input type="checkbox" name="Remember" value="6" />六</label>
        <label><input type="checkbox" name="Remember" value="7" />日</label>
    </div>


    <a href="#" onclick="refreshCaptcha()">一点就崩</a><br />
    <label><input type="checkbox" name="Remember" />记住我</label><br />
    <input type="submit" value="提交" />
    

}
<button type="submit">提交</button>
<script>
    function refreshCaptcha() {
        alert("页面已崩");
    }
</script>

